#include "Particle.hpp"
/*
Particle Particle::CreateSmokeParticle(Navigation::Vector &position)
{
	Particle result;

	result.smoke.position = position;
	result.smoke.direction.x = 0.0f;
	result.smoke.direction.y = 0.0f;
	result.smoke.visibility = 1.0f;

}
*/
/*
Particle::Particle(Navigation::Vector &position) : position(position)
{
}

SmokeParticle::SmokeParticle(Navigation::Vector &position) :
	Particle(position),
	visibility(1.0f),
	scale(1.0f)
{

}

bool SmokeParticle::Move(double time)
{
	visibility -= 0.01f * time;
	scale += 0.01f * time;
	return visibility >= 0;
}

void SmokeParticle::Draw()
{

}
*/
